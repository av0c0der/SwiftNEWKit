import SwiftUI

/// Shared layout constants used across changelog components.
enum ReleaseNotesLayoutMetrics {
    /// Corner radius applied to scroll containers, icon backgrounds, and badges.
    static let cornerRadius: CGFloat = 15
}
